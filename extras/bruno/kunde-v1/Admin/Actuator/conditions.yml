info:
  name: conditions
  type: http
  seq: 6

http:
  method: GET
  url: "{{actuatorUrl}}/conditions"
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5


