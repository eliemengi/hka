info:
  name: sbom
  type: http
  seq: 7

http:
  method: GET
  url: "{{actuatorUrl}}/sbom"
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5


